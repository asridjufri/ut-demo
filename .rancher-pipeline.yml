stages:
- name: compile
  steps:
  - runScriptConfig:
      image: gcc:latest
      shellScript: |-
        ls -lah
        git status
        git fetch --all --tags --prune
        git checkout tags/stable -b master
timeout: 60
